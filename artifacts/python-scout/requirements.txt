fastapi
uvicorn[standard]
httpx
pydantic
scrapy
playwright
beautifulsoup4
lxml
aiohttp
python-multipart
requests
tldextract
fake-useragent
python-dotenv
google-genai
tenacity
chardet

# ── Added May 2026 ──────────────────────────────────────────────────────────
# Crawl4AI: LLM-friendly crawler. Closes the existing gap — 6 TS files in
# the api-server already wrap a Crawl4AI engine but the Python sidecar never
# had it installed, so those code paths were silently no-op'ing.
crawl4ai>=0.4.0

# Selenium: per operator request. Note: Playwright (already installed) covers
# every Selenium use case and is faster + more stable. Use Selenium only when
# integrating an existing Selenium-based test suite or legacy code.
selenium>=4.20.0
